mqtt {
    max_packet_size = 1MB
    max_mqueue_len = 65535
    retry_interval = 10s
    keepalive_multiplier = 1.25
    property_size = 32
}

listeners.tcp {
    bind = "0.0.0.0:1883"
}

log {
    to = [console]
    level = info
}
